document.write(nombre);

let nombre = " ";
let apellido = "";
nombre = kelvin;


/*tips para no mostra toda informcion de los estados

1) git status -s (sirve para mostra el estaado del contenido)

*/


/*comndos para ver los cambios en vivvo
 1) git diff (permite ver los cambios)
 2) git diff --staged (vemos los archivos que estn)
 */

 /*como ver los commit 
 1) git log (muestra toda l informacion)
 2) git log --oneline (muestraa solo los commit que has ello)
 */

 /*forma para ver que rama estas  
 git branch (te permite ver que rama estas)
 
 */